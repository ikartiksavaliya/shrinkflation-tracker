# Food Price Inflation Analysis

A data analysis project to track and visualize food price trends over time using Python and Power BI.

## 📂 Repository Structure

* **`analysis.ipynb`**: The main Jupyter Notebook containing the data processing and analysis logic.
* **`PowerBI/`**: Contains the Power BI dashboard (`.pbix`) for visualizing price trends.
* **`data/`**: Directory for storing raw and processed datasets.
* **`requirements.txt`**: List of Python dependencies required to run the project.

## 🛠️ Tech Stack

* **Python** (Pandas, NumPy, Matplotlib)
* **Power BI** (Dashboarding & Visualization)
* **Jupyter Notebook**

## 🚀 Setup & Usage

1.  **Install Dependencies:**
    ```bash
    pip install -r requirements.txt
    ```

2.  **Run Analysis:**
    Open and execute the cells in `analysis.ipynb` to process the data.

3.  **View Dashboard:**
    Open the file inside the `PowerBI/` folder to interact with the visual reports.

## 📊 Project Goal

To analyze historical price data of food products and identify inflation trends across different categories.
